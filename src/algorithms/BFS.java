package algorithms;

import agent.Agent;
import agent.Node;

public class BFS extends Agent {
    public BFS(Node startNode, Node goalNode) {
        super(startNode, goalNode);
    }

    @Override
    public void search() {

    }
}
