package algorithms;

import agent.Agent;
import agent.Node;

public class AStar extends Agent {
    public AStar(Node startNode, Node goalNode) {
        super(startNode, goalNode);
    }

    @Override
    public void search() {

    }
}
