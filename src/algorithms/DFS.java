package algorithms;

import agent.Agent;
import agent.Node;

public class DFS extends Agent {
    public DFS(Node startNode, Node goalNode) {
        super(startNode, goalNode);
    }

    @Override
    public void search() {

    }
}
